Push the current state of the mulle-clang branch to GitHub and the mulle remote,
ensuring it lands in the correct branch (`MULLE_CLANG_BRANCH`).

This must happen before the release is built, so the GitHub release tag points
to the right commit.
